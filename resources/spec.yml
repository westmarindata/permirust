version: 1
adapter: postgres

roles:
  jdoe:
    can_login: yes
    is_superuser: no
    member_of:
      - analyst
    owns:
      schemas:
        - finance_reports
      tables:
        - finance_reports.Q2_revenue
        - finance_rerports.Q2_margin
    privileges:
      schemas:
        read:
          - finance
          - marketing
        write:
          - reports
      tables:
        read:
          - finance.*
          - marketing.ad_spend
      sequences:
        write:
          - reports.*
